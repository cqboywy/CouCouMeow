# UI 设计系统

方向为“凑凑喵的绘本乐园”：奶白背景、蜜桃粉、奶蓝与淡紫，低饱和、宽松、无广告。正式图标使用 Lucide，不使用 Emoji 代替功能图标。

主要触控区域至少 48×48px，主要按钮高度至少 52px。正文不小于 16px；页面每屏只突出一个主要动作。错误采用柔和杏色和可操作文案，不使用刺眼红色。

交互反馈不超过 200ms，仅使用 transform/opacity，并尊重 `prefers-reduced-motion`。iPad 是首要验证尺寸，手机和电脑保留完整功能。

